import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    default: {
        flex: 1,
        backgroundColor: 'orange',
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#fff'
    },
    marginBottom: mb => ({ marginBottom: mb }),
    boxLarge:{ width: '100%'},
    
})

export default styles
